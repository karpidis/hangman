# Changes - 2026-05-24

## Git Cleanup

### Problem
- `hangman_flask/hangman_venv/` (virtual environment) was accidentally committed to git
- Deleting it locally left hundreds of "deleted" files showing in `git status`
- Several new untracked files needed to be added

### Fix
1. Added `hangman_flask/hangman_venv/` to `.gitignore`
2. Ran `git rm -r --cached hangman_flask/hangman_venv/` to remove the venv from git tracking
3. Staged all deletions/modifications with `git add -u`
4. Added new untracked files:
   - `Hangman Terminal Version 1/` (de/, usermanager/, hangman_international_v2.py)
   - `chess_passgen/`
   - `db/pl/Polish.db`, `db/sw/Swahili.db`, `db/tr/Turkish.db`
   - `pass_generator.py`
   - `.gitignore` update

### Notes
- `users.db` files were intentionally excluded from commits (database files)
- `git commit -a` only stages already-tracked files; new files always need `git add` first
